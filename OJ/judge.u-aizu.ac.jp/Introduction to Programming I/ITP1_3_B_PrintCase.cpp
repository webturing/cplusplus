#include <bits/stdc++.h>

using namespace std;

int main() {
    for (int no = 1, x; cin >> x;) {
        if (x == 0) break;
        cout << "Case " << no++ << ": " << x << endl;
    }
    return 0;
}