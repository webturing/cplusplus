#include <iostream>
#include <cstdlib>
#include <algorithm>
using namespace std;
int main() {
  int data[10];
  for (int i = 0; i < 5; i++) cin >> data[i];
  sort(data, data + 5);
  for (int i = 0; i < 5; i++) cout << a[i] << endl;
  return 0;
}
