#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[]) {
  int n;
  cin >> n;
  if (n & 1) {
    cout << "no" << endl;
  } else {
    cout << "yes" << endl;
  }
  return 0;
}