#include <iostream>
#include <cstring>
using namespace std;
int f(int n) {
  char s[100];
  sprintf(s, "%d", n);
  int i, len = strlen(s);
  int max = 0;
  for (i = 0; i < len; i++) {
  }
}
int main() {

  for (int n; cin >> n, n; cout << f(n) << endl)
    ;
  return 0;
}
