#include<bits/stdc++.h>

using namespace std;

int main() {
    // ifstream cin("sample.out");
    int n, m, a;
    while (cin >> n >> m >> a)
        cout << 1LL * (n / a) * (m / a) << endl;
    // cin.close();
    return 0;
}